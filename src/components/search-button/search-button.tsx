import './search-button.scss';

export const SearchButton = () => {
  return (
    <button
      onClick={() => {
        //логика поиска
      }}
      className="search__button"
    >
      Search
    </button>
  );
};
